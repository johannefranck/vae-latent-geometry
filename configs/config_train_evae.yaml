training:
  encoder_seed: 12           # Seed for encoder & data split
  reruns: [0, 1, 2, 3, 4]    # Rerun IDs — only decoder reinitialized
  epochs: 200
  batch_size: 64
  lr: 0.001
  ensemble_sizes: [1, 2, 3]  # Number of decoders in ensemble

vae:
  input_dim: 50
  latent_dim: 2
